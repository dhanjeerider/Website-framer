/** Minimal Next.js config for static export */
const nextConfig = {
  reactStrictMode: true,
  // If you rely entirely on next export output:
  output: "export",
  // ...existing config if any...
};

export default nextConfig;
