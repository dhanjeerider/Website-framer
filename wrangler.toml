name = "mockupgen-ai"
compatibility_date = "2024-11-25"
# Next.js static export output (matches next export -o out)
pages_build_output_dir = "out"

# Note: removed the [build] block — Cloudflare Pages does not accept it in wrangler.toml.

